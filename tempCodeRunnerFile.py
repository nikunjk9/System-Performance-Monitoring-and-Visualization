    # cursor.execute('insert into Performance values (GETDATE(),'
    #                + str(cpu_usage) + ','
    #                + str(memory_usage) + ','
    #                + str(cpu_interrupts) + ','
    #                + str(cpu_calls) + ','
    #                + str(memory_used) + ','
    #                + str(memory_free) + ','
    #                + str(bytes_sent) + ','
    #                + str(bytes_received) + ','
    #                + str(disk_usage) + ')'
    #                )
    # con.commit()